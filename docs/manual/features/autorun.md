# 自动任务与调休

自动任务用于处理调休、作息表变更、课程表调整等需要按日期自动生效和恢复的场景。所有自动任务在管理端配置，服务端在客户端请求配置时根据日期条件动态应用。

## 四种任务类型

系统支持四种自动任务类型：

| 类型名称 | 说明 |
|----------|------|
| 调休（Compensation） | 将指定日期的工作日与节假日互换。例如国庆调休中，周六变为工作日（上周一的课），周一变为休息日。 |
| 作息表调整（Timetable） | 在指定日期当天临时更换作息时间表。例如运动会当天使用特殊的作息安排。 |
| 课程表调整（Schedule） | 在指定日期当天临时更换课程安排。例如期末考试期间更改当天的课表。 |
| 全部调整（ALL） | 同时应用作息表和课程表的调整，即类型 2 和类型 3 的组合。 |

每项任务包含触发日期、优先级、生效域和类型相关的配置参数。任务状态（待生效/生效中/已过期）由服务端根据当前日期与任务日期自动判断。

## 生效域

生效域（scope）决定自动任务应用于哪些班级。生效域采用三级层次结构：

- **学校级**（如 `39`）：对整个学校的所有班级生效。
- **年级级**（如 `39/2023`）：对特定年级（即入学年份为 2023 年的年级）的所有班级生效。
- **班级级**（如 `39/2023/1`）：仅对指定的单个班级生效。

管理端在创建任务时提供多选下拉（按学校/年级/班级三级选项），所选级别决定生效范围。系统会对生效域进行规范化处理，在任务列表中显示生效域的可读描述。

## 优先级排序

自动任务列表按以下规则排序：

1. **状态优先**：生效中的任务排在最前，其次为待生效任务，最后为已过期任务。
2. **优先级降序**：同一状态的任务中，优先级数值越大的排在越前面。

多个任务同时生效且存在冲突时，高优先级会覆盖低优先级的配置。系统不会自动检测任务间的配置冲突，创建任务时需要自行规划优先级。

> [!WARNING]
>
> 同一优先级中的冲突，服务端不保证执行顺序。

## 状态管理

> [!NOTE]
>
> 所有自动任务均为**单日规则**：任务只在其记录的日期当天生效，不存在「日期范围」。任务格式为「在日期 X 当天使用配置 Y」，到期自动恢复。

每项自动任务具有三种生命周期状态：

- **待生效**（状态标记为黄色）：当前日期尚未到达任务的生效日期，配置尚未应用。
- **生效中**（状态标记为绿色）：当前日期与任务的生效日期相同，配置已应用。
- **已过期**（状态标记为红色）：当前日期已超过任务的生效日期，配置已自动恢复为任务生效前的状态。

状态判断基于服务端的当前日期与任务的生效日期比较。任务过期后，服务端将其标记为已过期状态，不再将其配置纳入客户端的请求响应中。任务本身不会自动删除，便于日后查阅或重新启用。

## 调休数据

调休任务（类型 0）涉及节假日与工作日的双向转换。系统提供以下查询方式：

### 节假日与工作日双向查询

管理端在配置调休任务时，提供两个方向的查询接口：

- **根据节假日查工作日**：输入一个节假日期，系统返回该日期对应的补班日（工作日），帮助确定需要上课的日期。
- **根据工作日查休息日**：输入一个应上班但因调休变为休息日的日期，系统返回对应的休息说明。

两种查询均通过服务端 API 实时返回结果。

### 全年调休批量导入

![调休导入位置示意图](https://image-hk-1.oss-accelerate.aliyuncs.com/image-20260719030202306.png)

管理端提供全年调休批量导入功能。操作方法：

1. 进入“实用工具”页面，点击“打开调休导入”。
2. 选择目标年份，系统自动调用 API 获取该年份的全部节假日与工作日对应关系。
3. 系统逐条创建调休类型的自动任务，每条任务包含一个节假日→工作日的转换关系。

批量导入适用于学期初一次性配置全年的调休安排。

## 自动填充课程模板

在创建"课程表调整"或"全部调整"类型的任务时，系统提供自动填充课程模板功能。点击"自动填充"按钮后：

- 系统获取生效域内所有班级在目标工作日（星期几）的现有课表作为模板。
- 如果多个班级的课表一致，自动填充到任务配置中。
- 如果不同班级的课表节数不一致，弹出冲突提示框，列出差异详情。

自动填充可减少手动录入课表的工作量。

## 冲突检测

系统在保存任务前执行基本的冲突检测：

- **生效域冲突**：当多个任务覆盖同一个班级且生效日期相同时，系统不会自动阻止，但会在客户端渲染时按优先级选取生效的配置。建议在创建任务前检查已有任务列表，避免意外覆盖。
- **课表节数冲突**：在使用自动填充功能时，如果多个班级的课表节数不同，系统会提示冲突。此时需要手动选择其中一个班级的课表作为模板，或逐班单独创建任务。

冲突检测提供信息供操作者判断。复杂的多班级场景，建议逐班创建独立任务。

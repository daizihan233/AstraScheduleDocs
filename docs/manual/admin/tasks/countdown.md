> [!DANGER]
>
> 本页由 AI 工具参考代码编写，尚未经过人工审核，内容仅供参考。如果无法解决问题或需要协助部署，可邮箱联系：kuohu@getastra.cn

# 倒数日

路径：`/countdown`

管理客户端显示的倒数日/纪念日配置。每个倒数日配置包含生效域和多条日程项。

## 列表页面

表格展示所有倒数日配置，顶部搜索框支持按 ID、日程名称、日期筛选。

| 列 | 说明 |
|----|------|
| 唯一 ID | 配置数据库主键 |
| 生效域 | 配置生效的学校/年级/班级范围 |
| 状态 | 配置状态（如"生效中"/"已过期"） |
| 日程数量 | 该配置下包含的日程条数 |
| 快捷操作 | 修改、删除 |

点击行首的展开箭头，可查看该配置下的全部日程明细（名称、日期、优先级、状态）。

## 新增/编辑页面

路径：`/countdown/add` 或 `/countdown/edit/:id`

- **生效域**：多选下拉，选项按学校/年级/班级三级列出，选中上级后下级自动禁用。
- **日程列表**：动态增删的日程条目，每条包含：
  - **名称**：倒数日名称（如"期末考试""高考"）。
  - **日期**：日期选择器（格式 YYYY-MM-DD）。
  - **优先级**：数字，多条日程同时显示时的排序依据。

提交时验证：生效域不能为空，至少填写一条完整日程（名称和日期均不为空），日期格式必须为 YYYY-MM-DD。
